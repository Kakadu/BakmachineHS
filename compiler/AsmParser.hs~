module AsmParser (
  inter
) where


import Text.Parsec
import Text.Parsec.String
import Types

inter :: Stream s m Char => ParsecT s u m String
inter = string "int" 

--  do
--    
--    digits <- many1 digit
--    let i = read digits :: Int
--    return i
    
--    case Types.inter_of_int i of
--     Just x -> return x
--      Nothing -> unexpected "Bad interrupt code"
                 --newErrorMessage (newPos "asdf" 0 0) "Bad interrupt code"
                 

